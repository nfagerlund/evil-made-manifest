# This demonstrates that native types also work w/ defined function. This almost certainly includes custom native types, although I don't have one handy to test with. 
notice(defined(file))
notice(defined(fill))

